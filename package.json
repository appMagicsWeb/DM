{
  "name": "DM",
  "version": "1.0.0",
  "description": "",
  "main": "project/main.js",
  "scripts": {
    "start": "electron .",

    "buildAll": "electron-packager . --all --overwrite",
    "buildMac": "electron-packager ./ DM --platform=darwin --arch=x64 --icon=./icon.icns --overwrite --out ./dist",
    "buildWin": "electron-packager ./ DM --platform=win32 --arch=x64 --icon=./icon.ico --overwrite --out ./dist --version=1.6.6 --app-version=1.0.0"

  },
  "repository": "https://github.com/appMagicsWeb/DM",
  "author": "WaterTian",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.1.0",
    "electron-packager": "^8.6.0"
  }
}
